package Gui.TeamCRUD;

import javafx.geometry.Insets;
import javafx.scene.layout.VBox;

public class TeamUpdate{
    public static VBox getComponent() {
        // General settings
        VBox teamUpdateBox = new VBox(20);
        teamUpdateBox.setPadding(new Insets(20));


//        teamUpdateBox.getChildren().addAll();
        return teamUpdateBox;
    }
}
